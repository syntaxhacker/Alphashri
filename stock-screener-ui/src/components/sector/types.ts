// Sector Analysis component types
// This component is a simple iframe wrapper with no additional types needed currently.

export interface SectorPageProps {
  // Reserved for future props if needed
}
